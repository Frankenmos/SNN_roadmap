── WORKSTREAM 1: Repair the Streamlit dashboard (tools/analysis/dashboard.py) ──
Known root causes (from the 2026-07-02 instrumentation session — verify, then fix):
- The Ray training path never writes STEP or REWARD_COMP rows, so the Policy and Reward Shaping tabs are empty for every real run. Don't fake the missing data: instead surface what DOES exist in ppo_updates — rollout_policy_*_count, rollout_feedback_*, sil_loss / sil_gate_open_fraction / sil_buffer_size, grad_norm_{trunk,actor_head,critic_head,target_head} — as new panels (action-mix-over-updates, SIL health, grad-norm decomposition). Show a clear "Ray runs don't log per-step data" notice on the old tabs instead of silent emptiness.
- Guard the unconditional torchvision import (dashboard.py:14) so the dashboard runs without it (checkpoint kernel-grid view degrades gracefully).
- reward_components schema is legacy-6-component vs the live v4 reward: map or drop, don't mislabel.
Then harden: empty/short DBs must not crash any figure; cache staleness (st.cache_data keyed only by path — include file mtime); Streamlit deprecation warnings; models/ discovery should work regardless of CWD. Verify by launching the dashboard headlessly against a real run DB under models/ and exercising every tab (a small scripted smoke test is fine).

── WORKSTREAM 2: Checkpoint snapshot versioning + registry for .pth ──
Implement the planned snapshot scheme in the Ray training path (ray_train.py):
- Two-tier schedule (dense early, sparse later; both intervals config-gated) writing models/<run>/snapshots/policy_u{N}.pth containing policy weights + extractor/normalizer state + metadata (policy_version/update_count, episode, wall time, git commit, config hash) — NO optimizer state. Extractor state is mandatory in every snapshot (the count=0 lesson). Must not change checkpoint.pth/best_checkpoint.pth semantics or break resume.
Then build tools/registry/ (CLI):
- `list <run>` — table of snapshots with metadata and eval scores joined from training_logs.db where available.
- `show <snapshot>` — metadata, per-module param counts, learned alpha/beta summary (reuse tools/analysis/analyze_pth.py collectors).
- `diff <a> <b>` — layer-wise weight deltas (L2/cosine, sorted), config diff, metadata diff. Works across runs.
Integrate as a new dashboard tab (snapshot picker + diff view). Unit-test the schedule logic and the registry against synthetic checkpoints; verify CLI end-to-end on an existing real checkpoint.

── WORKSTREAM 3: Interactive 3D architecture explorer (the centerpiece) ──
A standalone frontend under tools/viz/arch_explorer/ — React + React Three Fiber + drei + Tailwind via Vite ("cyberpunk lab" aesthetic: dark, neon edges, glassmorphic panels). If node/npm is unavailable on this machine, fall back to a single self-contained HTML file with a vendored three.js. This tool is the visual half of the planned trace-through sessions: its purpose is rebuilding a mental model of the real system, so every zone, shape, formula, and code excerpt must be pulled from THIS repo (agent_core/, obs_space, docs/current/ARCHITECTURE.md; the 693-line historical version at commit 53566d8 has the detailed prose to mine).
1. ZONES (the real inference pipeline, laid out along a spline):
   PySC2 obs → ObservationExtractor (normalizers) → PolicyInputBatch [spatial 27×84×84, entity 24×F + mask, selection 20×7 + mask, action-feedback 1×12, meta 15] → per-group encoders (CNN → 7×7 pooled tokens + learned 2D pos; token-type embeddings) → 95-token stream (49+24+20+1+1) → spiking attention → fast & slow token-temporal SNN pathways (recurrent (syn, mem) carried across env steps — the real dual-timescale mechanism) → heads: action head (NO_OP / masked LEFT_CLICK / RIGHT_CLICK), coarse-to-fine spatial head (49-way coarse × 144-way fine, with the fine skip connection from pre-pool 84×84 conv features), value head → dispatch (Smart_screen(x,y) / no_op).
   Optional toggle layer for the training loop: rollout fragments → per-fragment GAE → TBPTT chunks (window 128) → PPO epochs → SIL trophy-buffer pass.
2. MOVING PARTS: input tensors as glowing particle streams along splines; token stream renders as 95 distinct particles color-coded by token type; the fast SNN pathway pulses at env-step rate, the slow pathway with visibly slower decay (tie the two rates to the alpha/beta time-constant story); spiking attention zone emits discrete spike flashes, not continuous glow.
3. INTERACTIVITY: hover = neon highlight + tooltip with tensor shape; click = smooth camera tween to the zone + slide-out glassmorphic info panel; scroll = camera dollies through the pipeline input→output.
4. INFO PANEL per zone: component name; REAL tensor shapes in/out; the actual math (LIF membrane update, surrogate gradient, attention over spikes, coarse×fine factorized categorical, PPO clip objective, SIL loss −sil_coef·(R−V)+·log π — KaTeX or clean HTML); "what it does and why it's built this way" in 3–6 engineering sentences (mine ARCHITECTURE.md + the historical long version for the why, e.g. why the fine skip connection exists); a real excerpt from the actual source with file:line reference — no placeholder code anywhere.
5. VERIFICATION: vite build must pass; headless render smoke test (e.g. puppeteer/playwright screenshot showing the canvas drew); window resize handled; use instancing and avoid per-frame allocations so it stays smooth on a real GPU. Add a README with run instructions.
Accuracy rule for all three workstreams: when the code and ARCHITECTURE.md disagree, the code wins — and note the discrepancy in docs/current/ARCHITECTURE.md.