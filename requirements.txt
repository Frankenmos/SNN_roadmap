# ============================================================
# PySC2 Spiking PPO Agent - core runtime dependencies
# ============================================================
# Curated list of *direct* dependencies, grouped by purpose.
# Transitive packages (e.g. scipy, pillow, pyarrow, the jupyter
# stack) are intentionally omitted and resolved by pip.
#
# For exact, fully-pinned versions used in development - including
# the CUDA nightly build of torch - see requirements-windows.txt,
# or install the torch build that matches your platform from
# https://pytorch.org/get-started/locally/.
#
# Dev/test tooling (pytest, ruff) lives in requirements-dev.txt.
# Recommended Python: 3.10 - 3.12.
# ------------------------------------------------------------

# --- Deep learning + spiking neural networks ---
torch>=2.4
torchvision>=0.19
snntorch>=0.9

# --- Numerics ---
numpy>=1.26

# --- StarCraft II environment (PySC2) ---
PySC2>=4.0
# protobuf is hard-pinned for PySC2 / s2clientprotocol compatibility
# on newer Python; do not bump without re-checking the env.
protobuf==3.20.3
absl-py>=2.0
dotmap>=1.3

# --- Distributed training ---
ray>=2.40

# --- Configuration ---
PyYAML>=6.0

# --- Analysis, metrics & dashboard ---
pandas>=2.0
matplotlib>=3.8
seaborn>=0.13
plotly>=5.0
scikit-learn>=1.4
streamlit>=1.40
